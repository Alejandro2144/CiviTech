# ─── Base image ────────────────────────────────────────────────────────────────
FROM python:3.11-slim
# 📌 Aquí: si quieres otra versión de Python, cámbiala arriba.

# ─── Configuración del contenedor ─────────────────────────────────────────────
WORKDIR /app

# No bufferizar la salida de Python
ENV PYTHONUNBUFFERED=1

# ─── Dependencias ──────────────────────────────────────────────────────────────
COPY requirements.txt .
RUN pip install --upgrade pip \
 && pip install --no-cache-dir -r requirements.txt

# ─── Código de la aplicación ───────────────────────────────────────────────────
COPY . .

# ─── Variables de entorno parametrizables ──────────────────────────────────────
# Puerto en el que corre FastAPI (puede inyectarse desde K8s)
ARG PORT=8001
ENV PORT=${PORT}

# Expone el puerto configurado
EXPOSE ${PORT}

# ─── Comando de arranque ───────────────────────────────────────────────────────
# 📌 Si cambias la ruta al broker/MinIO, revisa las vars RABBIT_URL y MINIO_ENDPOINT
ENTRYPOINT ["sh","-c"]
CMD ["uvicorn main:app --host 0.0.0.0 --port $PORT"]
